import { Body, Controller, Post } from "@nestjs/common";

@Controller('user')
export class UserController {

    // @Post('zalo-token-login')
    // async zaloTokenLogin(@Body() { zaloAccessToken }: any): Promise<{ accessToken }> {
        
    // }
}